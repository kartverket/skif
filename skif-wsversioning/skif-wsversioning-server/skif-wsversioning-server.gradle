apply plugin: 'java'

configurations {
    client
}

dependencies {
    compile project(':skif-wsversioning:skif-wsversioning-api')
    compile project(':skif-store-server')

    provided libraries.jee

    testCompile project(':skif-testsupport-common')

    // internal dependencies + optional deps to logging frameworks:
    testCompile project(':skif-config')
}
