apply plugin: 'maven'

dependencies {
    compile libraries.guice_with_used_deps
    provided libraries.jaxws_rt
}

task testJar(type: Jar) {
    classifier = 'tests'
    from sourceSets.test.classes
}

configurations {
    testApi
}

artifacts {
    testApi testJar
}